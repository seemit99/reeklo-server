# PostgreSQL 연결 (prod: Cloud SQL)
DATABASE_URL=postgresql://mystic_user:password@localhost:5432/mystic
# 필수 (32자 이상 랜덤 문자열) — 생성 예: openssl rand -base64 48
JWT_SECRET=change-me-to-a-random-secret-at-least-32-chars
# 토큰 만료 (ms)
JWT_EXPIRATION=86400000
PORT=8080
